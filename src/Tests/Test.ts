// Archivo de prueba libre
// se corre con el bat "test" que corre el build-test
// y luego ejecuta el .js de este archivo

import { CustomAuth } from '../CustomAuth/CustomAuth';

let obj = new CustomAuth();

console.log(obj);